export enum FriendRequestsStatusEnum {
  PENDING = 0,
  ACCEPTED = 1,
  REJECTED = 2,
}

export enum FriendRequestsTypeEnum {
  WEB = 0,
}

export enum GenderEnum {
  MALE = 0,
  FEMALE = 1,
  UNKNOWN = 2,
}

export const TOKEN_STORAGE_KEY = 'online-chat-token';

export const REQUEST_SUCCESS_CODE = 0;
